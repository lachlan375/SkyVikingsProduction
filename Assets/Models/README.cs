using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class README : MonoBehaviour {

    /* 1. Create Empty Object as Child to Main Camera
     * 2. Add Camera Component & Water Mask Script
     * 3. Change Camera Component Tag to "Untagged", Clear Flags to "Solid Color", Background to 0,0,0, Culling Mask to "WaterMask", Depth to -2, disable HDR (optional)
     * 6. Add "Water" Script & WaterMat Material (Assets\Materials) to the 100x100 Plane acting as Water */

}
